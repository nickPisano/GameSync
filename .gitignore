# Rust
/target
**/*.rs.bk
Cargo.lock.orig

# GameSync local runtime data (when run with GAMESYNC_DATA pointing here)
/.gamesync

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Signing secrets — never commit
*.p12
*.cer
*.key
